<%= form_for(@invoice) do |f| %>
	<%= f.text_field :name %><br/>
	<%= f.text_area :address %><br/>
	<%= f.text_field :cp %><br/>
	<%= f.text_field :rfc %><br/>
	<%= f.check_box :favorite %><br/>
	<%= f.submit "Guardar" %>
<% end %>